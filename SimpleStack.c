#include <stdio.h>

int size;
	
void simple_stack_push(char *stack,int size)
{
	stack[size]=c;
	size++;		
}		

char simple_stack_pop(char *stack)
{
	size--;
	return stack[size];
}

int main()
{
	char stack[size];
	
	return 0;
}
